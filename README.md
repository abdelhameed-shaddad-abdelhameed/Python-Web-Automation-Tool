# 🤖 Python Web Automation Bot
A powerful automation script built with **Python** to perform repetitive web tasks efficiently.

### ✨ Features
* **Browser Automation:** Simulates real human behavior (clicks, typing, scrolling).
* **Headless Mode:** Runs in the background without opening a browser window.
* **Data Extraction:** Scrapes data and saves it to local files.
* **Error Handling:** Automatically handles connection errors and timeouts.

### 🛠️ Built With
* **Python 3.x**
* **Playwright / Selenium**

### 🚀 How to Run
1. Install dependencies:
   ```bash
   pip install -r requirements.txt
